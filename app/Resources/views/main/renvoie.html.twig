{% extends 'base.html.twig' %}

{% block stylesheets %}

    {{ parent() }}

{% endblock %}

{% block body %}

    {{ parent() }}

     <div class="container-fluid ">

        <div id="commande">
            <h3>Renvoie de commande</h3>
            <div class="well">
                {{ form_start(renvoieCommandeForm) }}
                <div class="form-group">
                    <div class="col-sm-2">
                        {{ form_label(renvoieCommandeForm.dateEntree) }}
                    </div>
                    <div class="col-sm-10">
                        {{ form_widget(renvoieCommandeForm.dateEntree) }}
                        {{ form_errors(renvoieCommandeForm.dateEntree) }}
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-2">
                        {{ form_label(renvoieCommandeForm.email) }}
                    </div>
                    <div class="col-sm-10">
                        {{ form_widget(renvoieCommandeForm.email, { 'attr':  {
                            'class': 'form-control',
                        }}) }}
                        {{ form_errors(renvoieCommandeForm.email) }}
                    </div>
                </div>
                <input class="btn btn-primary" id = "next" type ="submit" value="Rechercher"/></input>
                <div id ="renvoie"><a href="{{ path('homepage') }}">Retourner à l'accueil</a></div>

                {{ form_end(renvoieCommandeForm) }}
            </div>
        </div>

     </div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/script.js') }}" type="text/javascript"></script>

{% endblock %}